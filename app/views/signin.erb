<h2>Sign In</h2>

<p><form action="/sign-in" method="POST">
  <select name="user">
  <% @users.each do |user| %>
    <option value="<%= user.id %>"><%= user.name %></option>
  <% end %>
  </select>
  <input class="btn btn-primary btn-sm" type="submit" value="Sign In">
</form></p>