source "https://rubygems.org"

gem "sinatra"
# We added the three gems below to our Gemfile. 
gem "activerecord"
gem "sinatra-activerecord"
gem "rake"

group :development do
  gem "sqlite3" # We also added the 'sqlite3' gem to our development group - we'll use this to setup our database.
  gem "pry"
  gem "shotgun"
  gem "tux"
end

